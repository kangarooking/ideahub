"""Prompt templates for AI processing."""

SUMMARIZE_PROMPT = """你是一个内容摘要助手。请将用户提供的内容精炼为一段简洁的摘要（100-200字）。
要求：
- 保留核心观点和关键信息
- 语言简洁流畅
- 不要添加评论，只做客观总结
直接输出摘要内容，不要添加标题或前缀。"""

EXTRACT_TAGS_PROMPT = """你是一个内容标签提取助手。请从用户提供的内容中提取 3-8 个关键标签。
要求：
- 标签应反映内容的核心主题、领域、关键概念
- 每个标签 2-6 个字
- 以 JSON 数组格式返回，如：["标签1", "标签2", "标签3"]
- 只返回 JSON 数组，不要其他内容"""

SUGGEST_IDEAS_PROMPT = """你是一个内容创作灵感顾问。基于用户提供的素材内容，给出 3-5 个创作灵感方向。
要求：
- 每个方向用一句话描述，包含具体的内容角度或切入点
- 方向应该有差异性，覆盖不同的创作视角（如深度分析、观点评论、实操教程、案例拆解等）
- 用编号列表格式输出
- 每个方向后可附加简短说明（为什么这个方向有价值）"""

QUERY_EXPANSION_PROMPT = """用户搜索："{query}"

请分析用户的搜索意图，生成 5-8 个扩展搜索关键词（包含同义词、相关概念、上下位词）。

严格按照以下 JSON 数组格式返回，不要添加任何其他文字或代码块标记：
["关键词1", "关键词2", "关键词3"]"""

SEMANTIC_RERANK_PROMPT = """用户搜索意图："{query}"

以下是候选素材列表：
{candidates_json}

任务：评估每个素材与用户搜索意图的语义相关度，打分 0-100。

评分标准：
- 90-100：直接讨论用户搜索的主题，内容匹配度极高
- 70-89：与搜索主题密切相关，有明确关联
- 50-69：有一定相关性，但不是直接相关
- 30-49：弱相关，只有边缘关联
- 0-29：不相关（不要返回）

严格按照以下 JSON 数组格式返回，不要添加任何其他文字或代码块标记：
[{{"id": 1, "score": 95, "reason": "一句话说明匹配原因"}}, {{"id": 2, "score": 78, "reason": "原因"}}]

要求：
1. 只返回相关度 >= 30 的结果
2. 按分数从高到低排列
3. reason 用简短中文说明匹配原因
4. 直接返回 JSON 数组，绝对不要用 ``` 包裹"""
